spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        generate_statistics: false

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    root: INFO
    org.hibernate.SQL: INFO
    org.springframework.security: INFO

ratelimit:
  enabled: true